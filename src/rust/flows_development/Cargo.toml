[package]
name = "flows_development"
version = "0.1.0"
edition = "2024"

[dependencies]
harness_engine = { path = "../harness_engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
