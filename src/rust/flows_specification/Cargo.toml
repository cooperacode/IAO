[package]
name = "flows_specification"
version = "0.5.0"
edition = "2024"
[dependencies]
harness_engine = { path = "../harness_engine" }
serde_json = "1"
sha2 = "0.10"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
