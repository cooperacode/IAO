# TodoApp WebAPI

ASP.NET Core Web API for a Postgres-backed to-do list, organized by vertical slices.

## Bootstrap and verify

Run the single verification command from this directory:

```bash
./init.sh
```

The script starts the local Postgres service through Docker Compose, restores the solution,
and runs all tests.
