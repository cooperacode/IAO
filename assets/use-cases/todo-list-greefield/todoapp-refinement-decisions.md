# Human Refinement Decisions — TodoApp WebAPI

## Product context

- TodoApp WebAPI is **greenfield**.
- There is no prior implementation whose JSON file persistence needs to be migrated.
- Data migration, JSON file reading, and compatibility with a legacy contract are out of
  scope.
- The mention of a JSON migration in the context of `adr-0001-vertical-slice.md` is obsolete
  context and does not change the greenfield scope defined by the brief.

## Origin

Decision confirmed by the requester when refinement resumed.
