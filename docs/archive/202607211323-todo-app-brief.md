# App de Lista de Tarefas (To-Do) — WebAPI com persistência em Postgres

**Tipo:** Greenfield

## Contexto
Este caso descreve um app de lista de tarefas exposto como **WebAPI HTTP**, com
persistência em **Postgres**, subindo localmente via **Docker Compose** — não é um serviço
de nuvem, é infraestrutura de desenvolvimento/teste que roda na máquina de quem está
construindo o projeto. O documento é usado como exemplo e demonstração do fluxo de
transformar um brief em features implementáveis; é autocontido, não pressupõe nenhuma
implementação anterior.

## Objetivo
Expor a lista de tarefas (criar, listar, concluir, editar, remover, filtrar) via
**endpoints HTTP**, persistindo as tarefas em **Postgres**, com o banco subindo via
`docker-compose.yml` versionado no projeto. Os testes automatizados combinam dois níveis:
**testes de unidade** (regras de negócio puras de cada slice, sem HTTP nem Postgres) e
**testes de integração** (o endpoint completo, via HTTP, rodando contra uma instância real
do Postgres). A implementação é em **.NET** (C#), com um projeto **ASP.NET Core Web API** e
dois projetos de testes automatizados (unidade e integração). Deve existir **um único
comando de verificação** que suba a infraestrutura e rode as duas suítes de testes.

## Arquitetura
A organização do código segue **Vertical Slice Architecture**: cada funcionalidade abaixo
(endpoint) é uma fatia vertical autocontida — parsing da requisição HTTP, regra de negócio e
acesso ao Postgres num único lugar coeso — em vez de camadas horizontais
(Controller/Service/Repository) compartilhadas entre endpoints. Só entidade, conexão com o
banco e exceções de domínio são compartilhadas entre slices. Ver a decisão completa em
[ADR-0001](adr-0001-vertical-slice.md) e a visão de componentes em
[c4-diagrama-componentes.md](c4-diagrama-componentes.md).

## Funcionalidades desejadas (prioridade sugerida)
1. **Subir a infraestrutura** — `docker-compose.yml` com um único serviço Postgres; o
   schema (tabela de tarefas) é criado automaticamente na subida (script de inicialização
   ou migração aplicada pela própria aplicação ao conectar).
2. **Adicionar tarefa** — `POST /tasks`; título obrigatório; a tarefa nasce "pendente";
   persistida no Postgres.
3. **Listar tarefas** — `GET /tasks`; retorna id, status (pendente/concluída) e título,
   lidos do Postgres.
4. **Concluir tarefa** — `PATCH /tasks/{id}/complete`; marca uma tarefa como concluída no
   Postgres.
5. **Persistência** — as tarefas sobrevivem entre reinicializações do processo da API,
   graças ao Postgres.
6. **Remover tarefa** — `DELETE /tasks/{id}`; apaga uma tarefa no Postgres.
7. **Editar título** — `PUT /tasks/{id}`; altera o texto de uma tarefa existente no
   Postgres.
8. **Filtrar listagem** — `GET /tasks?status=...`; mostrar só pendentes, só concluídas, ou
   todas.
9. **Testes de integração** — cada endpoint acima é coberto por um teste que chama a API via
   HTTP e roda contra o Postgres real subido pelo compose (sem mocks, sem repositório em
   memória).
10. **Testes de unidade** — as regras de negócio puras de cada slice (ex.: título obrigatório,
    transições de status válidas, filtro de listagem) são cobertas por testes de unidade
    rápidos, isolados de HTTP e do Postgres.

## Regras / restrições
- **Stack obrigatória**: .NET/C# para aplicação e testes (ex.: ASP.NET Core Web API +
  xUnit); Postgres como banco (driver/ORM à escolha da implementação, ex.: Npgsql, Dapper ou
  EF Core).
- **Organização em vertical slices** (ver [Arquitetura](#arquitetura)): cada endpoint é uma
  fatia isolada, sem Service/Repository genéricos compartilhados entre endpoints.
- **Infraestrutura via Docker Compose**: um `docker-compose.yml` versionado sobe um único
  serviço Postgres local. Sem serviços de nuvem, sem autenticação de usuário na aplicação.
- **Conexão configurável**: string de conexão via variável de ambiente (ex.:
  `TODO_DB_CONNECTION`), com um valor padrão que aponta para o serviço subido pelo compose.
- **Ids estáveis e previsíveis** (ex.: sequenciais, gerados pelo Postgres). Operar sobre um
  id inexistente devolve um **erro HTTP claro** (ex.: `404 Not Found`), não quebra a API.
- **Título vazio é rejeitado** com resposta de erro HTTP clara (ex.: `400 Bad Request`).
- **Testes de integração são reais**: cada endpoint precisa ser verificável isoladamente por
  um teste automatizado que chama a API via HTTP e roda contra o Postgres real — nada de
  mock ou repositório em memória substituindo o banco.
- **Testes de unidade cobrem a regra de negócio pura de cada slice** (validação, transições
  de status, filtro), sem subir HTTP nem Postgres — complementam, não substituem, os testes
  de integração.
- **Isolamento entre testes**: cada teste de integração limpa ou isola os próprios dados
  (ex.: transação com rollback ao final, ou limpeza da tabela entre casos) para não vazar
  estado entre testes.
- O projeto deve ter um `init.sh` idempotente que suba o Postgres (`docker compose up -d
  --wait`, aguardando o banco ficar pronto) e em seguida rode o comando único de
  verificação (preferencialmente `dotnet test` na raiz da solution, cobrindo os dois
  projetos de teste).

## Fora de escopo (por enquanto)
- Múltiplos usuários, sincronização em nuvem, login.
- Datas de vencimento, lembretes, prioridades, tags.
- Interface gráfica (frontend) — só a API HTTP nesta fase, sem UI.
- Orquestração além de um único container Postgres (sem Kubernetes, sem múltiplos
  serviços, sem ferramenta de migração dedicada obrigatória).
- Ambiente de CI/produção com Postgres gerenciado — o compose é só para desenvolvimento e
  teste local.

## Critério de "pronto"
Todos os endpoints acima funcionam via HTTP contra o Postgres subido via
`docker-compose.yml`, os dados **persistem entre reinicializações do processo da API**
(sobrevivem a reiniciar o programa, não necessariamente a um `docker compose down -v`), e as
suítes de **testes de unidade e de integração passam inteiras (verdes)** — a de integração
rodando contra o banco real, cobrindo cada endpoint.

## Cenários de teste (Gherkin)
Cenários de aceitação por funcionalidade, para orientar os testes de integração (via HTTP
contra o Postgres real) e de unidade (regra de negócio pura) de cada slice.

### Funcionalidade: Adicionar tarefa (`POST /tasks`)

```gherkin
Funcionalidade: Adicionar tarefa
  Como usuário da API
  Quero criar uma nova tarefa
  Para acompanhar algo que preciso fazer

  Cenário: Caminho feliz - criar tarefa com título válido
    Dado que o Postgres está disponível e vazio
    Quando eu envio POST /tasks com título "Comprar leite"
    Então a resposta tem status 201 Created
    E o corpo da resposta contém um id gerado, status "pendente" e título "Comprar leite"
    E a tarefa passa a existir no Postgres com esses mesmos dados

  Cenário: Fluxo de exceção - título vazio é rejeitado
    Dado que o Postgres está disponível
    Quando eu envio POST /tasks com título ""
    Então a resposta tem status 400 Bad Request
    E nenhuma tarefa é persistida no Postgres

  Cenário: Fluxo de exceção - título ausente no corpo da requisição
    Dado que o Postgres está disponível
    Quando eu envio POST /tasks sem o campo "titulo" no corpo
    Então a resposta tem status 400 Bad Request
    E nenhuma tarefa é persistida no Postgres

  Cenário: Fluxo alternativo - título com espaços nas extremidades é normalizado
    Dado que o Postgres está disponível e vazio
    Quando eu envio POST /tasks com título "  Comprar leite  "
    Então a resposta tem status 201 Created
    E o título persistido é "Comprar leite", sem espaços nas extremidades
```

### Funcionalidade: Listar tarefas (`GET /tasks`)

```gherkin
Funcionalidade: Listar tarefas
  Como usuário da API
  Quero listar as tarefas cadastradas
  Para saber o que já foi registrado

  Cenário: Caminho feliz - listar tarefas existentes
    Dado que existem 2 tarefas no Postgres: "Comprar leite" (pendente) e "Pagar conta" (concluída)
    Quando eu envio GET /tasks
    Então a resposta tem status 200 OK
    E o corpo da resposta contém as 2 tarefas, cada uma com id, status e título

  Cenário: Fluxo alternativo - lista vazia
    Dado que não existe nenhuma tarefa no Postgres
    Quando eu envio GET /tasks
    Então a resposta tem status 200 OK
    E o corpo da resposta é uma lista vazia

  Cenário: Fluxo de exceção - Postgres indisponível
    Dado que o Postgres está indisponível
    Quando eu envio GET /tasks
    Então a resposta tem status 500 Internal Server Error ou 503 Service Unavailable
    E o corpo da resposta não expõe detalhes internos de conexão com o banco
```

### Funcionalidade: Concluir tarefa (`PATCH /tasks/{id}/complete`)

```gherkin
Funcionalidade: Concluir tarefa
  Como usuário da API
  Quero marcar uma tarefa como concluída
  Para sinalizar que ela já foi feita

  Cenário: Caminho feliz - concluir tarefa pendente existente
    Dado que existe uma tarefa pendente com id 1 e título "Comprar leite"
    Quando eu envio PATCH /tasks/1/complete
    Então a resposta tem status 200 OK
    E o corpo da resposta mostra a tarefa 1 com status "concluída"
    E o status da tarefa 1 no Postgres passa a ser "concluída"

  Cenário: Fluxo de exceção - id inexistente
    Dado que não existe nenhuma tarefa com id 999 no Postgres
    Quando eu envio PATCH /tasks/999/complete
    Então a resposta tem status 404 Not Found
    E nenhum dado é alterado no Postgres

  Cenário: Fluxo alternativo - concluir tarefa já concluída é idempotente
    Dado que existe uma tarefa com id 1 e status "concluída"
    Quando eu envio PATCH /tasks/1/complete
    Então a resposta tem status 200 OK
    E o status da tarefa 1 no Postgres continua "concluída"
```

### Funcionalidade: Remover tarefa (`DELETE /tasks/{id}`)

```gherkin
Funcionalidade: Remover tarefa
  Como usuário da API
  Quero remover uma tarefa
  Para tirá-la definitivamente da lista

  Cenário: Caminho feliz - remover tarefa existente
    Dado que existe uma tarefa com id 1 e título "Comprar leite"
    Quando eu envio DELETE /tasks/1
    Então a resposta tem status 204 No Content
    E a tarefa 1 não existe mais no Postgres

  Cenário: Fluxo de exceção - id inexistente
    Dado que não existe nenhuma tarefa com id 999 no Postgres
    Quando eu envio DELETE /tasks/999
    Então a resposta tem status 404 Not Found

  Cenário: Fluxo alternativo - remover tarefa já concluída
    Dado que existe uma tarefa com id 1 e status "concluída"
    Quando eu envio DELETE /tasks/1
    Então a resposta tem status 204 No Content
    E a tarefa 1 não existe mais no Postgres
```

### Funcionalidade: Editar título (`PUT /tasks/{id}`)

```gherkin
Funcionalidade: Editar título da tarefa
  Como usuário da API
  Quero alterar o título de uma tarefa existente
  Para corrigir ou detalhar melhor o que preciso fazer

  Cenário: Caminho feliz - editar título de tarefa existente
    Dado que existe uma tarefa com id 1 e título "Comprar leite"
    Quando eu envio PUT /tasks/1 com título "Comprar leite integral"
    Então a resposta tem status 200 OK
    E o corpo da resposta mostra a tarefa 1 com título "Comprar leite integral"
    E o título da tarefa 1 no Postgres passa a ser "Comprar leite integral"

  Cenário: Fluxo de exceção - id inexistente
    Dado que não existe nenhuma tarefa com id 999 no Postgres
    Quando eu envio PUT /tasks/999 com título "Qualquer coisa"
    Então a resposta tem status 404 Not Found

  Cenário: Fluxo de exceção - título vazio é rejeitado
    Dado que existe uma tarefa com id 1 e título "Comprar leite"
    Quando eu envio PUT /tasks/1 com título ""
    Então a resposta tem status 400 Bad Request
    E o título da tarefa 1 no Postgres permanece "Comprar leite"

  Cenário: Fluxo alternativo - editar tarefa já concluída não altera o status
    Dado que existe uma tarefa com id 1, título "Comprar leite" e status "concluída"
    Quando eu envio PUT /tasks/1 com título "Comprar leite integral"
    Então a resposta tem status 200 OK
    E o status da tarefa 1 no Postgres continua "concluída"
```

### Funcionalidade: Filtrar listagem (`GET /tasks?status=...`)

```gherkin
Funcionalidade: Filtrar listagem de tarefas por status
  Como usuário da API
  Quero filtrar a listagem por status
  Para ver só o que ainda preciso fazer, ou só o que já concluí

  Cenário: Caminho feliz - filtrar por pendentes
    Dado que existem tarefas com status "pendente" e "concluída" no Postgres
    Quando eu envio GET /tasks?status=pendente
    Então a resposta tem status 200 OK
    E o corpo da resposta contém somente as tarefas com status "pendente"

  Cenário: Fluxo alternativo - filtrar por concluídas
    Dado que existem tarefas com status "pendente" e "concluída" no Postgres
    Quando eu envio GET /tasks?status=concluida
    Então a resposta tem status 200 OK
    E o corpo da resposta contém somente as tarefas com status "concluída"

  Cenário: Fluxo alternativo - sem filtro retorna todas as tarefas
    Dado que existem tarefas com status "pendente" e "concluída" no Postgres
    Quando eu envio GET /tasks sem o parâmetro status
    Então a resposta tem status 200 OK
    E o corpo da resposta contém todas as tarefas, independente do status

  Cenário: Fluxo de exceção - valor de status inválido
    Dado que existem tarefas no Postgres
    Quando eu envio GET /tasks?status=urgente
    Então a resposta tem status 400 Bad Request
    E nenhuma tarefa é retornada no corpo da resposta
```

### Funcionalidade: Persistência entre reinicializações

```gherkin
Funcionalidade: Persistência das tarefas no Postgres
  Como usuário da API
  Quero que minhas tarefas sobrevivam a um reinício do processo da API
  Para não perder o que já cadastrei

  Cenário: Caminho feliz - tarefas sobrevivem ao reinício do processo
    Dado que existe uma tarefa com id 1 e título "Comprar leite" persistida no Postgres
    Quando o processo da API é reiniciado, mantendo o container do Postgres no ar
    E eu envio GET /tasks
    Então a resposta tem status 200 OK
    E a tarefa 1 continua presente com os mesmos dados

  Cenário: Fluxo alternativo - schema é recriado automaticamente em um banco novo
    Dado um Postgres recém-subido pelo docker-compose, sem nenhuma tabela criada
    Quando a API se conecta ao banco pela primeira vez
    Então a tabela de tarefas é criada automaticamente
    E GET /tasks retorna 200 OK com uma lista vazia
```
